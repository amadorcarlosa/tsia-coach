import { AGENTS, RECENT_RUNS, RUN_SUMMARY } from "./demo-data";
import StepTimeline from "./StepTimeline";

export default function RunsTab() {
  return <div className="mx-auto flex max-w-console flex-wrap items-start gap-5 p-6"><aside className="mt-panel min-w-0 max-w-75 p-3.5" style={{ flex: "1 1 260px" }}><h2 className="mt-eyebrow px-1.5 pb-2.5 pt-1">Agents</h2><div className="grid gap-1">{AGENTS.map((agent, index) => <button key={agent.name} className={`rounded-md border px-3 py-2.5 text-left focus-visible:outline-2 focus-visible:outline-primary-500 ${index === 0 ? "border-primary-500 bg-primary-500/8" : "border-transparent hover:bg-bg-inset"}`}><span className="block text-sm font-semibold">{agent.name}</span><span className="mt-0.5 block text-xs text-ink-sub">{agent.description}</span></button>)}</div><div className="my-3.5 h-px bg-edge" /><h2 className="mt-eyebrow px-1.5 pb-2">Recent runs</h2><div className="grid gap-0.5 font-(--font-code) text-xs">{RECENT_RUNS.map((run, index) => <button key={run.id} className={`flex justify-between rounded-sm px-2 py-1.5 text-left text-ink-sub hover:bg-bg-inset focus-visible:outline-2 focus-visible:outline-primary-500 ${index === 0 ? "bg-bg-inset text-ink" : ""}`}><span>{run.id}</span><span className={run.status === "ok" ? "text-green-600" : "text-red-600"}>{run.status}</span></button>)}</div></aside><div className="grid min-w-0 gap-4" style={{ flex: "999 1 480px" }}><section className="mt-panel px-5.5 py-5"><div className="flex flex-wrap items-start justify-between gap-6"><div><div className="flex items-center gap-2.5"><h2 className="text-xl font-semibold">Scaffold Author</h2><span className="rounded-sm bg-green-600/12 px-1.5 py-0.5 text-[0.625rem] font-semibold uppercase tracking-wider text-green-600">completed</span></div><div className="mt-1.5 font-(--font-code) text-xs text-ink-muted">{RUN_SUMMARY.id} · {RUN_SUMMARY.model} · started {RUN_SUMMARY.started} · {RUN_SUMMARY.duration}</div></div><div className="flex gap-6 text-right"><div><div className="text-[0.6875rem] uppercase tracking-[0.18em] text-ink-muted">Tokens</div><div className="mt-1 font-(--font-code) text-lg">{RUN_SUMMARY.tokens}</div></div><div><div className="text-[0.6875rem] uppercase tracking-[0.18em] text-ink-muted">Est. cost</div><div className="mt-1 font-(--font-code) text-lg text-primary-600">{RUN_SUMMARY.cost}</div></div></div></div></section><StepTimeline /></div></div>;
}
